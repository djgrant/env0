{
  "name": "env0-monorepo",
  "workspaces": [
    "package",
    "example"
  ],
  "scripts": {
    "build": "bun --filter env0 build",
    "release": "scripts/release.sh",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.7",
    "env0": "workspace:*"
  }
}
